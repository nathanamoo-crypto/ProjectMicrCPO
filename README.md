# ChequeProcessingSystem
Your collaborator should:

Clone the project.

Open appsettings.json.

Change just this line to match their own SQL Server instance:

"MicrDbConnection": "Server=localhost\\SQLEXPRESS;Database=MicrDB;Trusted_Connection=True;TrustServerCertificate=True;"


That’s it.
